from __future__ import annotations
import sys
from importlib.machinery import ModuleSpec, PathFinder
from importlib.machinery import all_suffixes as module_suffixes
from importlib.util import spec_from_file_location
from itertools import chain
from pathlib import Path

MAPPING: dict[str, str] = {'suitkaise': '/Users/ctaro/projects/code/Suitkaise/suitkaise', 'suitkaise-examples': '/Users/ctaro/projects/code/Suitkaise/suitkaise-examples', 'suitkaise-noncode': '/Users/ctaro/projects/code/Suitkaise/suitkaise-noncode'}
NAMESPACES: dict[str, list[str]] = {'suitkaise-examples': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples'], 'suitkaise-noncode': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-noncode'], 'suitkaise-examples.pokerML': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML'], 'suitkaise-examples.language-scraper': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/language-scraper'], 'suitkaise-examples.pokerML.runs': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs'], 'suitkaise-examples.pokerML.runs.pokerml_run_1769047153': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1769047153'], 'suitkaise-examples.pokerML.runs.pokerml_run_1768990118': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1768990118'], 'suitkaise-examples.pokerML.runs.pokerml_run_1769138967': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1769138967'], 'suitkaise-examples.pokerML.runs.pokerml_run_1769129186': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1769129186'], 'suitkaise-examples.pokerML.runs.pokerml_run_1769234477': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1769234477'], 'suitkaise-examples.pokerML.runs.pokerml_run_1769235797': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1769235797'], 'suitkaise-examples.pokerML.runs.pokerml_run_1768988070': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1768988070'], 'suitkaise-examples.pokerML.runs.pokerml_run_1768987116': ['/Users/ctaro/projects/code/Suitkaise/suitkaise-examples/pokerML/runs/pokerml_run_1768987116'], 'suitkaise.suitkaise-docs.sk': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/suitkaise-docs/sk'], 'suitkaise.suitkaise-docs.processing': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/suitkaise-docs/processing'], 'suitkaise.suitkaise-docs.circuits': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/suitkaise-docs/circuits'], 'suitkaise.suitkaise-docs.cucumber': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/suitkaise-docs/cucumber'], 'suitkaise.suitkaise-docs.paths': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/suitkaise-docs/paths'], 'suitkaise.suitkaise-docs.timing': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/suitkaise-docs/timing'], 'suitkaise.cucumber._int': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/cucumber/_int'], 'suitkaise.cucumber._int.worst_possible_object': ['/Users/ctaro/projects/code/Suitkaise/suitkaise/cucumber/_int/worst_possible_object']}
PATH_PLACEHOLDER = '__editable__.suitkaise-0.4.0b0.finder' + ".__path_hook__"


class _EditableFinder:  # MetaPathFinder
    @classmethod
    def find_spec(cls, fullname: str, path=None, target=None) -> ModuleSpec | None:  # type: ignore
        # Top-level packages and modules (we know these exist in the FS)
        if fullname in MAPPING:
            pkg_path = MAPPING[fullname]
            return cls._find_spec(fullname, Path(pkg_path))

        # Handle immediate children modules (required for namespaces to work)
        # To avoid problems with case sensitivity in the file system we delegate
        # to the importlib.machinery implementation.
        parent, _, child = fullname.rpartition(".")
        if parent and parent in MAPPING:
            return PathFinder.find_spec(fullname, path=[MAPPING[parent]])

        # Other levels of nesting should be handled automatically by importlib
        # using the parent path.
        return None

    @classmethod
    def _find_spec(cls, fullname: str, candidate_path: Path) -> ModuleSpec | None:
        init = candidate_path / "__init__.py"
        candidates = (candidate_path.with_suffix(x) for x in module_suffixes())
        for candidate in chain([init], candidates):
            if candidate.exists():
                return spec_from_file_location(fullname, candidate)
        return None


class _EditableNamespaceFinder:  # PathEntryFinder
    @classmethod
    def _path_hook(cls, path) -> type[_EditableNamespaceFinder]:
        if path == PATH_PLACEHOLDER:
            return cls
        raise ImportError

    @classmethod
    def _paths(cls, fullname: str) -> list[str]:
        paths = NAMESPACES[fullname]
        if not paths and fullname in MAPPING:
            paths = [MAPPING[fullname]]
        # Always add placeholder, for 2 reasons:
        # 1. __path__ cannot be empty for the spec to be considered namespace.
        # 2. In the case of nested namespaces, we need to force
        #    import machinery to query _EditableNamespaceFinder again.
        return [*paths, PATH_PLACEHOLDER]

    @classmethod
    def find_spec(cls, fullname: str, target=None) -> ModuleSpec | None:  # type: ignore
        if fullname in NAMESPACES:
            spec = ModuleSpec(fullname, None, is_package=True)
            spec.submodule_search_locations = cls._paths(fullname)
            return spec
        return None

    @classmethod
    def find_module(cls, _fullname) -> None:
        return None


def install():
    if not any(finder == _EditableFinder for finder in sys.meta_path):
        sys.meta_path.append(_EditableFinder)

    if not NAMESPACES:
        return

    if not any(hook == _EditableNamespaceFinder._path_hook for hook in sys.path_hooks):
        # PathEntryFinder is needed to create NamespaceSpec without private APIS
        sys.path_hooks.append(_EditableNamespaceFinder._path_hook)
    if PATH_PLACEHOLDER not in sys.path:
        sys.path.append(PATH_PLACEHOLDER)  # Used just to trigger the path hook
